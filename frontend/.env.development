# Use Vite dev-server proxy to avoid CORS when calling production API from localhost.
# Requests to http://localhost:5173/api/* are proxied to https://api.baodarius.me/*
# Override in .env.local to use a local backend (e.g. VITE_API_URL=http://127.0.0.1:8000)
VITE_API_URL=http://localhost:5173/api
