/**
 * Centralized app/model version for badge and footer.
 * Single source of truth for "Model v1.2 – Feb 2026" display.
 */
export const APP_VERSION = "v1.2.0";
export const MODEL_DATE = "Feb 2026";

/** Display string for footer and scan result badge (single consistent place). */
export const VERSION_BADGE = `Model ${APP_VERSION} – ${MODEL_DATE}`;
